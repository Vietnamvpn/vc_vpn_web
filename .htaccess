<IfModule mod_rewrite.c>
    Options -Indexes
    RewriteEngine On

    # Chặn duyệt thư mục và bảo vệ mã nguồn gốc, đẩy request vào vc_public
    RewriteCond %{REQUEST_URI} !^/vc_public/
    RewriteRule ^(.*)$ vc_public/$1 [L]
</IfModule>